import { localMetadata, LocalPage } from "@/lib/local-page";

export const metadata = localMetadata("strony-internetowe-bielsko-biala");

export default function Page() {
  return <LocalPage slug="strony-internetowe-bielsko-biala" />;
}
