import { localMetadata, LocalPage } from "@/lib/local-page";

export const metadata = localMetadata("strony-internetowe-czechowice-dziedzice");

export default function Page() {
  return <LocalPage slug="strony-internetowe-czechowice-dziedzice" />;
}
