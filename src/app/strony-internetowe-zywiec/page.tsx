import { localMetadata, LocalPage } from "@/lib/local-page";

export const metadata = localMetadata("strony-internetowe-zywiec");

export default function Page() {
  return <LocalPage slug="strony-internetowe-zywiec" />;
}
