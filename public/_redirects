# KODA — przekierowania Cloudflare Pages.
# www → apex: reguła host-based poniżej jest POPRAWNA składniowo, ale CF Pages
# ignoruje ją w tym setupie (zweryfikowane 2026-08-27 regułą diagnostyczną:
# path-only działa, host-based nie — mimo www podpiętego jako custom domain).
# WŁAŚCIWY fix: zone-level Redirect Rule w dashboardzie CF (Rules → Redirect
# Rules): hostname equals www.kodastrony.pl → 301 https://kodastrony.pl$1.
# Regułę zostawiamy — gdyby CF naprawił routing, zadziała bez deployu.
https://www.kodastrony.pl/* https://kodastrony.pl/:splat 301
